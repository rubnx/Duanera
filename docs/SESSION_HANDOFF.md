# Session Handoff

## Current snapshot

Duanera is a greenfield customs/import-export data product.

Confirmed decisions:

- Chile-first.
- Spanish-first.
- Neon-hosted PostgreSQL as the MVP database provider.
- Drizzle as database layer.
- Raw source files in private Cloudflare R2 object storage.
- Postgres-first MVP.
- ClickHouse-ready architecture, but no ClickHouse dependency yet.
- No database-provider auth/storage/realtime/edge functions/generated APIs by default.
- Strong raw/provenance/normalization separation.

Main unresolved items:

- Chile Aduana official sources have initial local research archives under `data/sources/chile-aduana/`.
- Exact final field mapping and data granularity still need source-by-source validation.
- Public Aduana main files expose importer/exporter anonymous correlative IDs, not legal importer/exporter names or RUTs. `docs/research/CHILE_ADUANA_IDENTITY_INFERENCE_PILOT.md` documents a manual possible-identity workflow: exporter inference is promising when bulto marks/product text expose company evidence; importer inference is weaker and should stay internal until validated.
- Aduana.cl transparency evidence strengthens this conclusion. Responses `AE007T0002141` and `AE007T0002876`, plus Consejo para la Transparencia decisions `C1002-17` and `C3165-17`, indicate that names/RUTs linked to import/export transactions are excluded from open data or withheld under secrecy/reserve reasoning. A sampled Aduana.cl `ingresos_2002.zip` operational CSV also has only aggregate/statistical columns and no identity fields.
- Older Aduana.cl transparency registers are a real pre-datos.gob.cl lead. The April/September 2016 Aduana notices confirm a transition from transparency-request delivery to Datos Abiertos. Sampled 2012/2013 response registers and downloaded responses show complete monthly import/export databases and filtered extracts were delivered by CD, Excel, and RAR attachments through transparency requests. The public response documents do not expose the actual attached datasets/CD contents, so this proves prior request-specific access but not a public retrievable identity source. Evidence is stored under `data/research/aduana-transparency-archives/`.
- `docs/research/CHILE_ADUANA_IDENTITY_INFERENCE_SYSTEM.md` defines the V1 direction for DataSur-style possible importer/exporter identity. Aduana correlatives remain anonymous source identifiers; possible company names/RUTs must come from a separate evidence-backed inference layer with confidence, review status, and provenance. `docs/DECISIONS.md` decision 7 records this as product/data-identity policy.
- Wayback Machine checks of old Aduana.cl public statistics pages found archived import/export/statistics pages and one downloadable workbook sample, but the links and workbook contents are aggregate by country/product/series rather than identity-bearing transaction records. Keyword CDX checks for Aduana.cl URLs containing `importador`, `exportador`, or `rut` from 2000-2016 returned zero 200-status captures. Evidence is stored under `data/research/wayback-aduana-cl/`.
- Wayback Machine checks of datos.gob.cl found archived 2015/2016 CKAN Aduana dataset pages and the Aduana data dictionary page. The archived pages list monthly RAR item-record resources and XLSX metadata/dictionary resources, not legal identity files or correlative-to-RUT/name lookup tables. Exact CDX checks for sampled binary download URLs returned zero archived 200-status captures. Evidence is stored under `data/research/wayback-datos-gob-cl/`.
- `scripts/research/chile_aduana_identity_validation.py` now implements the V1 historical identity validation pipeline. It writes outputs under `data/research/chile-aduana-identity-validation/`; the current sampled run found 4,627 anonymous fingerprints and 0 historical named fingerprints.
- `scripts/research/chile_aduana_historical_acquisition.py` now acquires official historical datos.gob.cl resources for identity validation. January 2003, 2010, and 2015 imports and exports were downloaded, extracted, and manifest-preserved. Multipart RAR imports require `unar`; `bsdtar` alone can list some volumes but cannot reliably extract them. The downloader now resumes partial `.download` files after transient datos.gob.cl failures.
- `/explorer` received a focused UX pass: result summary metrics now render above the table, core numeric headers sort directly through existing query params plus new ascending modes for FOB, quantity, and gross weight, the filter submit button shows a React form pending state, pagination exposes 25/50/100 row-size links, advanced filters are grouped into geografia/logistica/rangos/orden instead of an 8-column strip, column help popovers are touch-capable, and the development-data badge is derived from source/period metadata rather than visible page rows.
- The design system was unified on a single OKLCH `--ds-*` token set (Shadcn tokens alias it), with refreshed palette, data-viz/sequential color ramps, Geist Mono + tabular numerals, and motion/elevation tokens (decision 19). A follow-up explorer interaction pass added: a sticky table header (the table-container is now a bounded `max-h-[70vh]` scroll region so `thead` pins on vertical scroll), a Cómoda/Compacta density toggle (`ExplorerDensityToggle`, persisted in `localStorage`, sets `--ds-table-row-height`/`--ds-table-cell-y` on the table shell), and a ⌘K command palette (`ExplorerCommandPalette`) for jump-to navigation, import/export flow shortcuts, and dynamic "filtrar por partida"/"buscar producto" actions. Sticky first column is intentionally deferred (needs per-cell hover/selection background coordination).
- A "less overwhelming / friendlier / accessible" explorer pass then reduced the first-screen load: the `Desglose del resultado` ranking is now a `localStorage`-persisted collapsible (`ExplorerDisclosure`, default collapsed); saved searches/history moved out of a permanent below-table panel into a compact toolbar popover (`ExplorerSearchesPopover`, replacing the old `ExplorerSearchMemory`); a single consolidated `ExplorerColumnGlossary` button supplements the hover-only per-column help; a dismissible `FirstStepsHint` onboarding banner (⌘K / views / glossary tips, persisted) sits under the header; the empty state has friendlier copy plus a "Probar guardadas" action; the results count is a polite `role="status"` live region; and the filter chip remove control has a larger (24px) tap target. The inline per-column `ExplorerColumnHelp` buttons were then removed from the table header (the consolidated `ExplorerColumnGlossary` is now the single source; sort links stay), eliminating ~11 redundant keyboard/screen-reader tab stops. The `ExplorerSearchesPanelContent` export remains for reuse inside the popover.
- A visual-calm pass reduced "too big / messy" above the table: the `Explorador` h1 dropped from `text-ds-xl` (22px) to `text-ds-lg-plus` (18px) with a shorter muted description; `ExplorerSummaryMetric` lost its redundant sub-note line (kept only label + value, label now small uppercase) so the summary collapsed from 3 lines per metric to 2; and the filter form, active-filter chips, and the results-summary strip are now merged into a single bordered surface (wrapper `div` holds the `form` + warnings + summary; summary is an attached `bg-ds-subtle/40` strip with `rounded-b-ds-md` and `border-t`). The wrapper intentionally does NOT use `overflow-hidden` because the absolutely-positioned "Más filtros" advanced-filters popover overflows below it and would otherwise be clipped (verified: panel renders full-height past the wrapper). Net result: two visual zones — the query panel and the results table — plus the collapsed ranking below.
- Auth provider is not chosen.
- Cloudflare R2 is the selected object storage provider for preserved source archives. The planned private bucket is `duanera-source-archive`.
- ClickHouse timing depends on actual data volume and query needs.
- Neon project `Duanera` now has a `dev` branch created from `production`. Local `.env.local` points at the dev branch; production should remain reserved for approved migrations and deployed app usage.
- Drizzle foundation exists with an initial provenance-only schema and generated migration. The first migration creates `source_files`, `import_batches`, `source_layouts`, and `source_layout_fields` only.
- The Drizzle schema now includes the first Aduana data foundation through `drizzle/0003_deep_iron_patriot.sql`: `raw_trade_rows`, `code_tables`, `code_values`, `source_trade_participants`, and `trade_records`.
- The implemented `trade_records` table is shared across imports/exports, provenance-linked to source file/import batch/raw row, and stores confirmed Aduana fields only. It intentionally does not include importer/exporter legal names, importer/exporter legal RUTs, company foreign keys, or ClickHouse as a dependency.
- Dev-only sample scripts exist for source file metadata, code-table seeding, raw row loading, and trade normalization. Data-mutating scripts require `DUANERA_DB_TARGET=dev`. The raw-row and normalization scripts are now batched/chunked for larger dev validation, but they still are not production ingestion.
- The Neon `dev` branch has been seeded with the current full March 2026 data foundation after the project was upgraded from the prior 512 MB limit. Current dev counts: 4 `source_files`, 2 `source_layouts`, 262 `source_layout_fields`, 12 `code_tables`, 881 `code_values`, 2 completed `import_batches`, 439,353 import `raw_trade_rows`, 109,187 export `raw_trade_rows`, 439,353 import `trade_records`, 109,187 export `trade_records`, and anonymous source participants for 15,491 importers, 3,461 primary exporters, and 7 nonzero secondary exporters. There are 0 raw parse failures, 0 orphaned trade records, 0 duplicate raw links, and 0 parsed raw rows missing trade records. The dev database is roughly 2 GB after storing full raw row snapshots and normalized records.
- `db:seed:source-layouts` now maps source-layout coded fields only to confirmed seeded dictionary keys such as `chile_aduana:aduanas`, `paises`, `puertos`, `moneda`, `vias_de_transporte`, and `unidades_de_medida`. Coded fields without a real seeded dictionary, such as `CODCOMUN`, remain coded but have `code_table_key = null` rather than invented keys. A dev reseed replaced the two March 2026 layout field sets and the distinct non-null layout keys now match seeded dictionary keys only.
- Full-month chronological list performance has a first dev optimization. The slow path was the generic `trade_records` list query joining every matching row to `raw_trade_rows` only to sort by source `row_number`. Migration `drizzle/0004_unknown_terrax.sql` adds `raw_trade_rows_trade_flow_period_row_number_idx` on `(trade_flow, period_year, period_month, row_number, id)`, and `src/trade/trade-records.ts` now uses a raw-row-ordered fast path for one-flow, one-exact-month list queries with no extra filters.
- After the optimization, database plans for the list query improved from roughly 14.7 seconds to 0.3 ms for March imports and from roughly 6.2 seconds to 22 ms for March exports. Local Next dev smoke checks on port 3001 returned 200 at roughly 1.1 seconds for the import API, 1.4-1.5 seconds for the export API, and 2.1 seconds for the import `/trade-records` page. Remaining overhead is mostly exact counts, code-label loading, Neon round trips, and Next dev rendering.
- A second dev-only query-performance pass hardened representative non-fast-path filters. Migration `drizzle/0005_certain_living_lightning.sql` enables `pg_trgm` and adds targeted `trade_records` indexes for origin country, destination country, customs office, transport mode, product text trigram search, and secondary exporter correlative. `src/trade/trade-records.ts` now uses raw-row ordered exact-month lists for compatible structured filters such as HS prefix, country, customs office, and transport mode, while product text and participant filters stay on the generic path.
- After the second pass, representative database plans improved: product text count for `motor` dropped from roughly 489 ms to 53 ms, origin/customs/transport counts moved from parallel sequential scans around 285-313 ms to index-only plans around 32-71 ms, and compatible structured list queries now use `raw_trade_rows_trade_flow_period_row_number_idx` with sub-millisecond to low-millisecond execution. Local API smoke checks returned 200 with roughly 0.9-1.2 seconds for structured filters, roughly 1.2 seconds for product text, and roughly 1.5 seconds for offset 100000.
- Cursor/keyset pagination now supplements offset pagination for raw-row ordered exact-month browsing. `src/trade/trade-records.ts` encodes/decodes opaque `after` cursors from raw row number and raw row id, `src/trade/trade-record-search.ts` parses `after`, and `/trade-records` uses returned `nextCursor` values for forward browsing. Existing `offset` URLs remain accepted for compatibility.
- Cursor performance comparison in dev: a raw-row ordered March import query with `offset 100000` took roughly 2.6 seconds in `EXPLAIN ANALYZE`; the equivalent `after` cursor predicate starting after raw row 100000 took roughly 8 ms. API smoke checks returned 200 for first cursor page, second cursor page, HS cursor-capable filter, and the `/trade-records` page. Invalid cursors return 400.
- Remaining pagination limits: product text and participant filters still use the generic path for now, and the demo UI only supports forward cursor navigation. Backward cursor navigation would require a cursor history stack or an explicit previous-cursor strategy.
- A read-only storage review of the March 2026 dev load measured the database at roughly 2.14 GiB. `raw_trade_rows` accounts for roughly 1.60 GiB total, including 1.43 GiB table storage and 170 MiB indexes. `trade_records` accounts for roughly 520 MiB total, including 342 MiB table storage and 178 MiB indexes. Code/source/provenance tables account for roughly 9 MiB. If the current design is kept unchanged, a simple linear estimate is about 25 GiB for 12 months and about 75 GiB for 36 months before additional countries, companion files, or production overhead.
- The storage review found that full raw row payloads are the main growth risk. A sampled raw row averages about 524 bytes of `raw_text`, 1,661 bytes of `raw_values`, and 211 bytes of trace metadata. Recommendation for the next design pass: keep source/provenance metadata and hashes in Postgres, but move full successful raw row payloads to object storage or reconstruct them from preserved source files, while retaining full payloads in Postgres only for dev/debug/error/sample cases.
- `docs/DECISIONS.md` decision 14 now records the raw-row retention policy. Official source files must be preserved outside Postgres forever. Postgres keeps raw row trace metadata, source/batch/row references, row hashes, parser status, and compact issue summaries. Full raw payloads should remain in Postgres only for dev samples, parser debugging, errors, warnings, sampled QA rows, or explicit audit examples. Successful high-volume payloads should be reconstructed from preserved source files or stored externally with a Postgres pointer/hash. No existing dev payloads should be deleted until a separate migration/backfill plan is approved.
- Migration `drizzle/0006_lovely_scrambler.sql` implements the first additive raw-row retention metadata on the Neon dev branch. `raw_trade_rows` now has payload retention mode, storage kind, optional storage bucket/key, payload hash, retained reason, prune timestamp, and reconstruction flag. Existing March 2026 dev rows were backfilled as `full_postgres` / `postgres` with `payload_hash_sha256 = row_hash_sha256`; read-only verification found all 548,540 rows have matching payload hashes and `payload_reconstructable = true`.
- Migration `drizzle/0007_dusty_jane_foster.sql` makes `raw_trade_rows.raw_text` and `raw_trade_rows.raw_values` nullable on the Neon dev branch. This enables post-normalization payload pruning while preserving trace metadata. Read-only verification confirmed both columns are nullable and all existing 548,540 March rows still have payloads.
- Future raw-row dev loads can set `RAW_ROW_PAYLOAD_RETENTION`. The default is `full_postgres`, preserving current behavior. `errors_and_warnings` records failed rows with `payload_retained_reason = parse_error`, warning rows as `parse_warning`, and successful rows as `pending_post_normalization_prune`. The current normalizer skips rows whose `raw_values` are already pruned.
- The raw-row sample loader now validates source-file `workingStorageKey` values before reading local files. It accepts only the first usable pipe-delimited path and requires that path to resolve inside the ignored repository `data/` archive.
- `npm run db:prune:raw-payloads` is now a dev-only post-normalization pruning script. It dry-runs unless both `-- --prune` and `RAW_ROW_PRUNE_CONFIRM=prune` are set. It prunes only `errors_and_warnings` / `pending_post_normalization_prune` rows with `parse_status = parsed`, no errors/warnings, unpruned payloads, `payload_reconstructable = true`, and a matching `trade_records` row with the same source file, import batch, flow, year, and month. A dry run on the current dev data found 0 candidates, as expected, because existing March rows are still `full_postgres`. No existing March 2026 dev payloads were deleted.
- The full retention/pruning flow was smoke-tested on a tiny isolated Neon dev sample under source file `retention_prune_smoke_1779999316642.txt`. One successful `errors_and_warnings` row with a matching `trade_records` row was pruned; one failed row and one warning row kept payloads. Verification confirmed the pruned row still has source file, import batch, row number, row hash, parser metadata, payload hash, `payload_retained_reason = pruned_after_normalization`, and a matching trade record. The `/trade-records/5f9c1cad-3167-4494-936a-788802d66169` detail page returned 200 and displayed the pruned-payload state. All 548,540 March 2026 `full_postgres` rows still have payloads.
- A read-only trade query service now exists at `src/trade/trade-records.ts`. It supports normalized filters over `trade_records` and joins provenance fields from source file, import batch, and raw row. A route-ready adapter exists at `src/trade/trade-record-search.ts`; it parses URL-style search params and returns API-shaped data/pagination/filter output. `npm run inspect:trade-records` prints a small import/export sample through this adapter.
- A Next.js App Router shell now exists under `src/app`. The first API route is `GET /api/trade-records`, implemented in `src/app/api/trade-records/route.ts`, and wired to the trade search adapter. Tailwind + shadcn/ui are installed. Internal demo pages now exist at `/trade-records` and `/trade-records/[id]`, using the trade service layer and decoded code-table labels. Local dev server was smoke-tested on port 3001 because port 3000 was already in use.
- The `/trade-records` MVP search UI now includes decoded lookup filters, flow-aware commercial range filters, active filter summaries, and conservative sort controls. Import item value ranges use CIF item; export item value ranges use FOB item. Declaration FOB, quantity, item gross weight, and total gross weight ranges are service-layer read-only filters. Non-source commercial sorts and range filters use offset pagination explicitly; exact one-flow/one-month source-order browsing keeps cursor pagination where compatible.
- The `/trade-records` search response now includes a read-only intelligence summary for the current filtered result set. It totals records, flow-aware item value, declaration FOB, quantity only when the unit is not mixed, item gross weight, and total gross weight. It also ranks top origin/destination countries, customs offices, relevant ports, and HS prefixes. The UI displays this above the table with decoded labels where code tables support them and keeps an explicit caveat that broad Postgres MVP aggregations may take longer.
- Trade-record searches now return internal demo timing metadata for list, summary, label enrichment, and total search time. The `/trade-records` UI shows a small Spanish performance note only when offset pagination is used, a broad high-volume result set has no narrowing filter, or the summary aggregation crosses the conservative slow threshold.
- `/trade-records` now supports low-risk drilldowns from summary rankings and result rows into existing filters for HS code, relevant country, customs office, relevant port, and anonymous Aduana importer/exporter correlatives. Drilldown links preserve the effective parsed search filters and clear cursor/offset pagination. `/trade-records/[id]` now shows capped read-only related-record groups for same HS/flow, same relevant country plus HS, same anonymous correlative, same customs office, and same relevant port. The UI keeps explicit copy that correlatives are anonymous Aduana identifiers, not verified legal company identities.
- `/trade-records` has a read-only commercial comparison view. The search response includes top comparison groups for HS/product, relevant country, customs office, relevant port, and anonymous Aduana correlatives, with records, flow-aware item value, FOB declaration, quantity only when units are comparable, gross weights, and average unit price only when currency and unit are comparable. Completely broad searches skip the heavier comparison aggregation and show Spanish copy asking users to narrow filters before comparing groups.
- `/trade-records` has URL-only commercial preset views that inherit the current default loaded period instead of pinning March 2026. The preset catalog lives in application code, opens normal shareable search URLs, uses only existing filter/query parameters, and includes convenience views for high-value imports/exports, selected product families, key countries, and logistics-heavy searches. Preset copy says these are convenience views, not verified market conclusions, and does not introduce company identity claims.
- `/sources` and `/sources/[id]` provide read-only provenance pages for official source files and import batches. They list raw-row counts, normalized trade-record counts, parser status, and safe links back into `/trade-records` by `sourceFileId` / `importBatchId`. The UI intentionally hides local paths, private storage keys, bucket URLs, and credentials while showing whether storage pointers exist.
- `/trade-records/[id]` now has a source-aware provenance panel that links to `/sources/[sourceFileId]`, the specific import-batch anchor on that source page, and source/batch-filtered `/trade-records` URLs. The panel shows sanitized source filename, source ID, import batch, parser/version, raw row reference, payload retention mode, storage kind, reconstructability, hash, and prune state without exposing local paths, R2 keys, bucket URLs, or credentials.
- `/trade-records` result rows now include compact source/trust indicators in the source column. The table links to the sanitized source file and batch context, exposes source/batch-filtered views, and shows batch status, payload retention, reconstructability, and parser/version without exposing local paths, R2 object keys, bucket URLs, credentials, or speculative company identity.
- `/research/identity-inference` is an internal-only, read-only review workflow for possible identity evidence. It is available in local development, and production access/navigation require `DUANERA_ENABLE_INTERNAL_RESEARCH=true`. It groups March 2026 records by anonymous Aduana importer/exporter correlative, shows record/declaration/HS/country counts, links back to `/trade-records` and source batches, and displays product/attribute/logistics evidence snippets with explicit caveats that they are unverified review signals, not legal names or RUTs. Export grouping expands primary and secondary exporter correlatives so review counts match the existing exporter filter semantics. `npm run research:identity-evidence` prints a CLI report and, for export samples, cross-checks local March 2026 export bulto companion marks without loading them into the database.
- `/data-quality` is an internal-only, read-only March 2026 quality and coverage dashboard. It computes raw-row vs normalized trade-record coverage by flow/source/batch, parse status, payload retention status, flow-aware high-value field coverage, decoded-label coverage for countries/aduanas/ports/transport modes, capped QA issue drilldowns for missing weights, undecoded logistics codes, missing/zero commercial values, and quantity/unit/price comparability, plus ranked source/batch remediation context. It links back to `/sources`, source/batch-filtered `/trade-records`, and representative record details, and keeps explicit caveats that anonymous correlatives are not legal identities and export CIF emptiness is not treated as an export defect.
- `/data-quality/field-mapping` is an internal-only, read-only field-mapping QA view for March 2026. It shows how selected official DIN/DUS raw columns map into normalized `trade_records` fields across commercial values, quantity/weight, logistics, HS/product, anonymous correlatives, and provenance. It distinguishes direct mappings, normalized/derived fields, and fields requiring review; exact coverage is measured on normalized columns, while raw source value coverage/samples are intentionally bounded to a small raw-row sample to avoid expensive broad JSON scans.
- `/data-quality/code-tables` is an internal-only, read-only code-table/data-dictionary QA view for March 2026. It ranks Aduana lookup gaps by commercial impact across countries, aduanas, ports, transport modes, units, currencies, and cargo types; shows source raw fields, normalized fields, expected code-table keys, decoded vs undecoded coverage, top undecoded codes, safe `/trade-records` samples, and source/batch provenance links without mutating code values or exposing local/R2 storage details.
- `/data-quality/remediation` is an internal-only, read-only remediation work queue for March 2026. It combines existing QA signals from field coverage, code-table coverage, source/batch issue counts, payload retention, and field mapping into a prioritized queue ranked by severity, MVP visibility, affected records, and signal confidence. It links back to the underlying QA pages, filtered `/trade-records`, and `/sources/[id]` contexts without changing data or implying legal company identity.
- `/data-quality/load-readiness` is an internal-only, read-only go/no-go gate for deciding whether to attempt another Aduana month in dev. It composes existing QA evidence from source/provenance coverage, parser raw-to-normalized counts, field mapping, code-table remediation, payload retention policy, trade-record performance guardrails, and the remediation queue. It classifies each area as ready/review/blocked and returns an overall `go`, `review-first`, or `no-go` decision without loading trade data, pruning payloads, exposing local/R2 paths, or making identity claims. After the reviewed evidence-code remediation and April payload pruning, March 2026 returns `review-first` with 2 ready areas, 5 review areas, and 0 blocked areas; April 2026 returns `review-first` with 3 ready areas, 4 review areas, and 0 blocked areas because payload retention is now ready; and the combined March+April range returns `review-first` with 2 ready areas, 5 review areas, and 0 blocked areas because March still retains 548,540 full raw payloads in Postgres. DIN import `grossWeightItem` is treated as an expected source limitation because DIN exposes `TOT_PESO` as total weight but no item-level gross-weight field. DUS code `0` for export transport/port and cargo type `S` are tracked as source-special/low-priority source values, not actionable dictionary blockers. The remediation queue is a checklist rollup, not an extra independent blocker.
- `npm run preflight:aduana-load` is now a read-only next-month Aduana load preflight. It can evaluate candidate local manifest rows or explicit working files without inserting source files, import batches, raw rows, trade records, pruning payloads, uploading R2 objects, or mutating code tables. The report checks local `data/` path safety, source provenance, period/flow consistency, raw/working size and SHA-256 checksums when present, DIN/DUS March 2026 field-count and no-header compatibility, bounded raw-row sample parse health, known dictionary-risk codes, payload-retention implications, and anonymous-correlative safety. A representative March dry run against `cl_aduana_imports_2026_03_raw.rar` and `cl_aduana_exports_2026_03_raw.rar` read 25 rows per flow, verified checksums, found 0 blockers, and returned overall `warning` due to existing coded-field dictionary coverage caveats and the recommendation to use `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` before loading another real dev month.
- The next chronological Aduana dev-month candidate, April 2026, has completed a controlled Neon dev load. On 2026-06-02, the four official datos.gob.cl April resources were downloaded into ignored local `data/`, preserving raw RAR files and extracted working TXT files for import main, export main, export bultos, and export transport documents. Ignored local source manifests, resource package evidence, and SHA-256 checksums were refreshed. The final April preflight by normalized raw filename for import main and export main returned 2 candidates, overall `warning`, 0 blockers, import sample 50/50 parsed with 178 fields, export sample 50/50 parsed with 84 fields, no headers detected, and raw/working checksums matching the local manifest. Two April main `source_files` were inserted in dev; companion bultos/transport-doc files were preserved locally but not loaded into `trade_records`. Raw-row loading used `DUANERA_DB_TARGET=dev` and `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings`, loading 408,027 import rows and 102,350 export rows with 0 parse failures. Normalization produced 408,027 April import `trade_records` and 102,350 April export `trade_records`, with 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and all 510,377 April raw rows still retained in Postgres as `pending_post_normalization_prune` (0 pruned). The period-scoped normalizer exposed that participant counters must be refreshed from aggregate `trade_records` truth rather than current-run in-memory stats; `scripts/ingest/normalize-trade-record-participants.ts` was hardened accordingly, and a one-off dev repair left participant-stat mismatches at 0. Correct April-only search URLs must use `periodFrom=2026-04&periodTo=2026-04`, not `period=2026-04`. April-only API/page smoke checks returned 200 with totals matching database counts; broad `/data-quality` and `/data-quality/load-readiness` pages also returned 200 but took roughly 22s and 14s respectively on March+April dev data. Do not load May or another month until April pruning readiness and March+April performance/storage impact are reviewed.
- An April raw-payload pruning readiness review on 2026-06-02 kept the database read-only and found April pruning is ready for a separate controlled dev prune pass. Neon dev measured roughly 4.35 GB total. `raw_trade_rows` measured roughly 3.32 GB total, including 1.10 GB table storage and 397 MB indexes, and `trade_records` measured roughly 1.01 GB total, including 662 MB table storage and 351 MB indexes. March remains `full_postgres` with 548,540 retained payload rows. April has 510,377 `errors_and_warnings` / `pending_post_normalization_prune` rows with raw text and raw values retained, 0 missing payload hashes, 0 non-reconstructable rows, and 0 pruned April rows. Serialized retained April payloads measured roughly 341 MB of `raw_text` plus 1.51 GB of `raw_values` text. `npm run db:prune:raw-payloads` dry-run returned 510,377 candidates, 510,377 eligible rows, 0 blocked rows, and 1 already-pruned old smoke row. Reconstructability sampling verified first/middle/last April rows for both import and export against preserved local working files: source line, retained `raw_text`, row hash, payload hash, field count, and parser health all matched. Representative March+April local Next dev timings: April import API 200 in about 3.7s with cursor pagination and slow-summary warning; April export API 200 in about 1.0s; broad March+April import API 200 in about 4.9s with offset/broad/slow warnings; broad March+April export API 200 in about 3.0s with offset/broad/slow warnings; filtered HS APIs 200 in about 0.7-0.9s. `/data-quality` and `/data-quality/load-readiness` still intentionally evaluate March 2026 evidence only and returned 200 in about 19s and 16s respectively on the March+April dev database. Do not load May or another month until April successful raw payloads are pruned or performance/storage risk is explicitly accepted.
- A controlled April raw-payload prune completed on 2026-06-02 against Neon dev only. The pre-prune checks confirmed `DUANERA_DB_TARGET=dev`, April preflight 0 blockers, 510,377 dry-run eligible rows, 0 blocked rows, and candidates only in April 2026: 408,027 import rows and 102,350 export rows. The prune was run in flow-scoped commands with explicit limits and `RAW_ROW_PRUNE_CONFIRM=prune`; export pruned 102,350 rows and import pruned 408,027 rows. Post-prune dry-run returned 0 candidates, 0 eligible, 0 blocked, and 510,378 already-pruned rows including the old smoke row. Verification found March still has 548,540 `full_postgres` rows with raw payloads retained; April has 510,377 `errors_and_warnings` rows marked `pruned_after_normalization`, with `raw_text` and `raw_values` null, `payload_pruned_at` set, payload hashes present, and `payload_reconstructable = true`. April import/export raw counts and `trade_records` counts remain 408,027 and 102,350, with 0 parse issues, 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 provenance mismatches. Representative April import/export detail pages returned 200 and display the pruned-payload messaging while preserving source/file/hash traceability. Immediate `pg_total_relation_size` did not shrink; `raw_trade_rows` measured about 3.33 GB with increased dead tuples after the update, so physical storage reclamation depends on normal Postgres/Neon vacuum behavior rather than this prune command alone. The logical storage risk is reduced because April no longer retains full successful raw payloads in row columns.
- A multi-month readiness audit for March+April made the product search safer before adding more months. `/trade-records` now reads available periods from `trade_records`, defaults unfiltered searches and quick presets to the latest loaded month, and tells users that the current dev scope is the loaded period range rather than implying March-only coverage. Period range searches still work, but the filter UI now warns that multi-month ranges use offset pagination and should be narrowed by month, HS, country, port, aduana, or commercial ranges for large result sets. The commercial comparison copy now refers to the filtered period.
- The internal QA workflows are now period-aware for March, April, or an explicit March+April range. `/data-quality`, `/data-quality/load-readiness`, `/data-quality/code-tables`, `/data-quality/field-mapping`, and `/data-quality/remediation` default to the latest loaded month, preserve `periodFrom`/`periodTo` across internal QA navigation, and keep Spanish copy clear that counts are scoped to the selected period. Field-mapping layout definitions still use the known DIN/DUS main-file baseline where appropriate, while raw/normalized coverage is scoped to the selected period. `/sources/[id]` now shows compact QA context for the source's own recorded period instead of a March-only baseline. `/research/identity-inference` remains a March-oriented identity-evidence research workflow until separately made period-aware.
- A follow-up May-readiness blocker audit clarified payload/storage QA after April pruning. `src/quality/data-quality.ts` now reports total raw rows separately from rows that still retain `raw_text`/`raw_values` and rows whose raw payload was pruned. `/data-quality` shows retained-vs-pruned payload counts and retained reasons, `/data-quality/load-readiness` only treats actually retained raw payload as a payload-retention review item, and `/data-quality/remediation` no longer queues reconstructable pruned rows as if they still retain full Postgres payload. Live dev checks on 2026-06-02 show April payload retention is ready with 510,377 pruned/reconstructable rows and 0 retained raw payload rows; March remains a historical dev review item with 548,540 retained `full_postgres` payload rows. Representative local Next dev timings on port 3001: March load-readiness about 14.7s, March+April load-readiness about 12.1s, April remediation about 8.3s, April code-tables about 3.6s, April field-mapping about 3.7s, April export API about 1.1s, and broad March+April API about 2.9s. These are acceptable for internal dev but still justify period scoping and smoke checks before loading May.
- A controlled Explorer data-foundation load on 2026-06-04 added the remaining current 2026 product-facing Aduana months available in the standard 2026 source manifest before March: January and February 2026. R2 snapshot preflight confirmed all official raw files, working files, and source manifests were already archived with matching size/SHA-256; remaining R2 missing objects are research/generated evidence that require separate review and are outside product ingestion. February loaded 354,508 import rows and 94,065 export rows into `trade_records`; January loaded 380,834 import rows and 124,062 export rows. Successful Jan/Feb raw payloads were loaded with `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and pruned after normalization: 448,573 February rows and 504,896 January rows now have `payload_retained_reason = pruned_after_normalization`, `raw_text = null`, and `raw_values = null`. Product-facing 2026 integrity checks show 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 pending prune rows. The only Jan/Feb parse issues are 14 retained export physical-line failures caused by five January and two February source records split across lines around `QASIM INTERNATIONAL`; those failed raw payloads remain retained with `payload_retained_reason = parse_error` for parser follow-up. `/explorer` still defaults to 2026-04 product data, not the 2099 smoke record. Representative dev reads after loading four 2026 months: an April 50-row Explorer-style page query took roughly 1.1s, and a four-month grouped summary took roughly 1.15s. Neon dev measured roughly 5.3 GB immediately after pruning; physical storage may lag logical pruning until normal Postgres/Neon maintenance. Typecheck and focused ingestion/search/provenance tests passed.
- The first historical backfill canary ran on 2026-06-04 for `2025-12` only, following `docs/superpowers/plans/2026-06-04-chile-aduana-backfill-readiness.md`. The official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired locally, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Final R2 preflight showed the 8 new 2025-12 raw/working objects as `already_archived`, with 0 remote mismatches, 0 unsafe objects, and 0 plan errors; remaining missing R2 objects are research/generated evidence outside product ingestion. Neon dev seeded only the 2025-12 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers; import sampled at 178 fields and export sampled at 84 fields with only known dictionary/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 426,414 parsed import rows, 106,396 parsed export rows, and 2 retained import parse-error rows. Normalization created 532,810 `trade_records` for 2025-12, exactly matching parsed raw rows. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 source/import-batch mismatches. Successful 2025-12 raw payloads were pruned after normalization; post-prune verification found 0 pending-prune rows, 532,810 parsed rows with `payload_retained_reason = pruned_after_normalization` and null `raw_text`/`raw_values`, and the 2 failed import rows still retaining raw payloads with `payload_retained_reason = parse_error`. The two failed physical rows are adjacent split-line import records at raw row numbers 311456 and 311457, with field counts 49 and 130 instead of 178. Product-facing period discovery now lists `2025-12` but still defaults Explorer to `2026-04`. Service smoke checks returned 25 rows for exact `2025-12` imports/exports and showed performance warnings for broad result sets; exact default `2026-04` and canary `2025-12` searches are in the same slow-but-warned Postgres MVP range, while a broad `2025-12` through `2026-04` query returned 25 rows over 2,545,196 records in about 13.6s with `offset_pagination`, `broad_result_set`, and `slow_summary` warnings. Neon dev measured about 6.956 GB immediately after the canary. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: continue 2025 backfill in one-month or at most two-month batches; because broad summaries are already slow at five months, prefer the next single month (`2025-11`) unless query-summary optimization is addressed first.
- A trade search performance pass on 2026-06-04 made Explorer broad searches safer before loading more 2025 months, without schema changes or new data loads. Root cause was repeated full-table aggregate/ranking work over multi-month, un-narrowed result sets, plus generic first-page list ordering over millions of rows. Product-facing un-narrowed multi-period searches now return a bounded summary state with the real total record count, skip expensive ranking/summary aggregation, show Spanish bounded-summary copy in `/explorer`, and emit `summary_bounded` instead of `slow_summary`. Exact single-month searches and narrowed multi-month searches still compute complete totals and rankings. The list path now uses segmented source-order raw-row queries for compatible first-page multi-period browsing. Representative service timings after the pass: exact `2026-04` import remained complete at about 9.7s wall with slow-summary warning; exact `2025-12` import remained complete at about 3.0s wall; broad `2025-12` through `2026-04` all-flow returned 25 rows over 2,545,196 records in about 1.1s with bounded summary; narrowed broad `hsCodePrefix=8471` plus China stayed complete at about 2.2s. Browser checks on port 3001 confirmed `/explorer` still defaults to `2026-04`, `2025-12` remains selectable, broad searches show bounded-summary/ranking copy, and exact/narrowed searches do not. Focused `test:trade-search` and `typecheck` passed. Remaining limits: exact unfiltered months still pay full summary/ranking cost, broad deep offset pagination remains bounded but not fundamentally solved, and full multi-month rankings require a single period or narrowing filters until a future materialized/analytics-store strategy is approved.
- The next historical backfill batch ran on 2026-06-04 for `2025-11` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired locally, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload found the 4 new 2025-11 official raw objects, 4 working objects, and refreshed historical source manifest snapshot missing; after guarded uploads, product/source archive classes showed 57/57 official raw, 52/52 working, and 12/12 source-manifest objects archived with 0 remote mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-11 main import/export `dataset_resource` source files. Preflight on those two main files had 0 blockers and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 376,853 parsed import rows, 90,141 parsed export rows, and 8 retained export parse-error rows. Normalization created 466,994 `trade_records` for 2025-11, exactly matching parsed raw rows; an initial normalizer attempt with `TRADE_RECORD_BATCH_SIZE=1000` hit query-size limits after 100 rows, and rerunning with `TRADE_RECORD_BATCH_SIZE=100` completed cleanly without schema changes. Successful 2025-11 raw payloads were pruned after normalization; post-prune verification found 0 pending-prune rows, 466,994 parsed rows with pruned payloads, and the 8 failed export rows retaining raw payloads with `payload_retained_reason = parse_error`. The failed physical rows are export split-line field-count errors at raw row numbers 8054/8055, 35372/35373, 51408/51409, and 79055/79056, with 19 or 66 fields instead of 84. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 source/import-batch mismatches. Product-facing period discovery now lists `2025-11` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-11` imports in about 3.8s and exports in about 1.1s, with exact summaries complete; a broad `2025-11` through `2026-04` search returned 25 rows over 3,012,190 records in about 1.6s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 11,914 records in about 3.1s. Browser checks on port 3001 confirmed default Explorer shows `2026-04` and not `2099-01`, exact `2025-11` renders without bounded-summary copy, and broad `2025-11` through `2026-04` shows bounded-summary copy. Neon dev measured about 8.037 GB immediately after this batch. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: `2025-10` is safe as the next single-month batch, but keep `TRADE_RECORD_BATCH_SIZE=100` unless the normalizer query-size issue is fixed, and keep watching recurring export split-line parse errors.
- The next historical backfill batch ran on 2026-06-04 for `2025-10` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired locally, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload found the 4 new 2025-10 official raw objects, 4 working objects, and refreshed historical source manifest snapshot missing; after guarded uploads, product/source archive classes showed 61/61 official raw, 56/56 working, and 12/12 source-manifest objects archived with 0 remote mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-10 main import/export `dataset_resource` source files. Preflight on those two main files had 0 blockers and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 439,947 parsed import rows, 96,417 parsed export rows, and 8 retained export parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-10` and `TRADE_RECORD_BATCH_SIZE=100`, creating 536,364 `trade_records` exactly matching parsed raw rows and touching 19,554 anonymous participants. Successful 2025-10 raw payloads were pruned after normalization; post-prune verification found 0 pending-prune rows, 536,364 parsed rows with pruned payloads, and the 8 failed export rows retaining raw payloads with `payload_retained_reason = parse_error`. The failed physical rows are export split-line field-count errors at raw row numbers 22726/22727, 30839/30840, 30842/30843, and 78826/78827, with 19 or 66 fields instead of 84. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 source/import-batch mismatches. Product-facing period discovery now lists `2025-10` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-10` imports in about 2.4s and exports in about 1.1s, with exact summaries complete; a broad `2025-10` through `2026-04` search returned 25 rows over 3,548,554 records in about 0.8s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 13,881 records in about 1.2s. Browser checks on port 3001 confirmed default Explorer shows `2026-04` and not `2099-01`, exact `2025-10` renders without bounded-summary copy, and broad `2025-10` through `2026-04` shows bounded-summary copy. Neon dev measured about 9.051 GB immediately after this batch. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: `2025-09` is safe as the next single-month batch, with the same constraints: keep `TRADE_RECORD_BATCH_SIZE=100`, archive R2 first, stop on preflight blockers, and keep monitoring recurring export split-line parse errors.
- The next historical backfill batch ran on 2026-06-04 for `2025-09` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired locally, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload found the 4 new 2025-09 official raw objects, 4 working objects, and refreshed historical source manifest snapshot missing; after guarded uploads, product/source archive classes showed 65/65 official raw, 60/60 working, and 12/12 source-manifest objects archived with 0 remote mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-09 main import/export `dataset_resource` source files. Preflight on those two main files had 0 blockers and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 418,087 parsed import rows, 90,243 parsed export rows, and 2 retained export parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-09` and `TRADE_RECORD_BATCH_SIZE=100`, creating 508,330 `trade_records` exactly matching parsed raw rows and touching 18,882 anonymous participants. One normalization run failed late during import upsert after 356,957 import records; a root-cause check found normal-sized contiguous rows at the failure boundary and the required `TRADE_RECORD_BATCH_SIZE=100` retry passed the same boundary and completed, so the failure behaved like a transient insert/database failure rather than a data-row blocker. Successful 2025-09 raw payloads were pruned after normalization; post-prune verification found 0 pending-prune rows, 508,330 parsed rows with pruned payloads, and the 2 failed export rows retaining raw payloads with `payload_retained_reason = parse_error`. The failed physical rows are export split-line field-count errors at raw row numbers 2854 and 2855, with 19 and 66 fields instead of 84. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 source/import-batch mismatches. Product-facing period discovery now lists `2025-09` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-09` imports in about 3.1s and exports in about 1.0s, with exact summaries complete; a broad `2025-09` through `2026-04` search returned 25 rows over 4,056,884 records in about 1.1s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 15,918 records in about 3.7s. Browser checks on port 3001 confirmed default Explorer shows the loaded range `2025-09` to `2026-04`, includes `2026-04`, excludes `2099-01`, exact `2025-09` import renders 418,087 results, and broad `2025-09` through `2026-04` shows bounded-summary context. Neon dev measured about 10.097 GB immediately after this batch. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: `2025-08` is safe as the next single-month batch, with the same constraints: keep `TRADE_RECORD_BATCH_SIZE=100`, archive R2 first, stop on preflight blockers, and keep monitoring recurring export split-line parse errors.
- The next historical backfill batch ran on 2026-06-04 for `2025-08` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired locally, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. The first import/export acquisition commands were run in parallel and raced the historical manifest write, leaving only the import row visible; rerunning the export acquisition serially repaired the manifest before any R2 upload or DB load. Snapshot R2 preflight before upload found the 4 new 2025-08 official raw objects, 4 working objects, and refreshed historical source manifest snapshot missing; after guarded uploads, product/source archive classes showed 69/69 official raw, 64/64 working, and 12/12 source-manifest objects archived with 0 remote mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-08 main import/export `dataset_resource` source files. Preflight on those two main files had 0 blockers and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 417,427 parsed import rows, 88,489 parsed export rows, and 0 parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-08` and `TRADE_RECORD_BATCH_SIZE=100`, creating 505,916 `trade_records` exactly matching parsed raw rows and touching 18,445 anonymous participants. Successful 2025-08 raw payloads were pruned after normalization; post-prune verification found 0 pending-prune rows, 505,916 parsed rows with pruned payloads, and no failed rows requiring retained payloads. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 source/import-batch mismatches. Product-facing period discovery now lists `2025-08` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-08` imports in about 2.7s and exports in about 0.9s, with exact summaries complete; a broad `2025-08` through `2026-04` search returned 25 rows over 4,562,800 records in about 0.9s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 18,190 records in about 1.0s. Browser checks on port 3001 confirmed default Explorer shows the loaded range `2025-08` to `2026-04`, includes `2026-04`, excludes `2099-01`, exact `2025-08` import renders 417,427 records, and broad `2025-08` through `2026-04` renders rows for the selected range. Neon dev measured about 11 GB immediately after this batch. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: `2025-07` is safe as the next single-month batch, with the same constraints: acquire import/export serially, keep `TRADE_RECORD_BATCH_SIZE=100`, archive R2 first, stop on preflight blockers, and keep monitoring recurring export split-line parse errors.
- The next historical backfill batch ran on 2026-06-04 for `2025-07` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload found the 4 new 2025-07 official raw objects, 4 working objects, and refreshed historical source manifest snapshot missing; after guarded uploads, product/source archive classes showed 73/73 official raw, 68/68 working, and 12/12 source-manifest objects archived with 0 remote mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-07 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 448,463 parsed import rows, 104,566 parsed export rows, and 2 retained import parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-07` and `TRADE_RECORD_BATCH_SIZE=100`, creating 553,029 `trade_records` exactly matching parsed raw rows and touching 19,324 anonymous participants. Successful 2025-07 raw payloads were pruned after normalization; post-prune verification found 0 prune candidates, 553,029 parsed rows with pruned payloads, and the 2 failed import rows retaining raw payloads with `payload_retained_reason = parse_error`. The failed physical rows are adjacent import field-count errors at raw row numbers 330534 and 330535, with 155 and 24 fields instead of 178. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, 0 source/import-batch mismatches, and 0 pending-prune rows. Product-facing period discovery now lists `2025-07` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-07` imports in about 2.8s and exports in about 1.2s, with exact summaries complete; a broad `2025-07` through `2026-04` search returned 25 rows over 5,115,829 records in about 1.1s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 20,385 records in about 1.2s. Browser checks on port 3001 confirmed default Explorer shows the loaded range `2025-07` to `2026-04`, includes `2026-04`, excludes `2099-01`, exact `2025-07` import renders 448,463 records, and broad `2025-07` through `2026-04` renders rows for the selected range. Neon dev measured about 12 GB immediately after this batch. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: `2025-06` is safe as the next single-month batch with the same constraints: acquire import/export serially, keep `TRADE_RECORD_BATCH_SIZE=100`, archive R2 first, stop on preflight blockers, and continue monitoring split-line parse errors.
- The next historical backfill batch ran on 2026-06-04/05 for `2025-06` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload found the 4 new 2025-06 official raw objects, 4 working objects, and refreshed historical source manifest snapshot missing; after guarded uploads, product/source archive classes showed 77/77 official raw, 72/72 working, and 12/12 source-manifest objects archived with 0 remote mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-06 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 400,855 parsed import rows, 87,001 parsed export rows, 2 retained import parse-error rows, and 4 retained export parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-06` and `TRADE_RECORD_BATCH_SIZE=100`, creating 487,856 `trade_records` exactly matching parsed raw rows, touching 17,927 anonymous participants, and touching 2,371 logistics parties. The first normalizer attempt exposed a performance bottleneck in the newer logistics-party enrichment path: aliases and record links were being upserted one row at a time. `scripts/ingest/normalize-trade-record-logistics-parties.ts` now caches repeated alias upserts during a run and batches logistics-party link upserts without changing schema or normalized output; focused logistics normalizer tests and typecheck passed before resuming. Successful 2025-06 raw payloads were pruned after normalization; post-prune verification found 0 prune candidates, 487,856 parsed rows with pruned payloads, and the 6 failed rows retaining raw payloads with `payload_retained_reason = parse_error`. Failed export rows are split-line field-count errors at raw row numbers 3000/3001 and 31147/31148, with 19 or 66 fields instead of 84. Failed import rows are adjacent field-count errors at raw row numbers 324711/324712, with 155 and 24 fields instead of 178. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, 0 source/import-batch mismatches, and 0 pending-prune rows. Product-facing period discovery now lists `2025-06` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-06` imports in about 3.0s and exports in about 1.2s, with exact summaries complete; a broad `2025-06` through `2026-04` search returned 25 rows over 5,603,685 records in about 3.5s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 22,680 records in about 2.8s. Browser checks on port 3001 confirmed default Explorer shows the loaded range `2025-06` to `2026-04`, includes `2026-04`, excludes `2099-01`, exact `2025-06` import renders 400,855 records, and broad `2025-06` through `2026-04` renders rows for the selected range. Neon dev measured about 13 GB immediately after this batch. Focused tests passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, `normalize-trade-record-logistics-parties.test.ts`, and `typecheck`. Recommendation: `2025-05` is safe as the next single-month batch with the same constraints: acquire import/export serially, keep `TRADE_RECORD_BATCH_SIZE=100`, archive R2 first, stop on preflight blockers, and continue monitoring split-line parse errors and logistics-party normalization runtime.
- The next historical backfill batch ran on 2026-06-05 for `2025-05` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload showed 160 archived objects, 86 missing non-current objects, 0 remote mismatches, 0 unsafe objects, and 0 errors; after guarded uploads of the 4 official raw objects, 4 working objects, and refreshed historical source-manifest snapshot, R2 preflight showed 169 archived objects, 77 missing non-current objects, 0 mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-05 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 404,234 parsed import rows, 91,872 parsed export rows, and 4 retained export parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-05` and `TRADE_RECORD_BATCH_SIZE=100`, creating 496,106 `trade_records` exactly matching parsed raw rows. The first normalizer attempt completed export and import through raw row 141,528 before a transient Neon/HTTP insert failure; a direct retry of rows 141,529-141,628 succeeded, and `scripts/ingest/normalize-trade-record-sample.ts` now has an explicit `NORMALIZE_START_ROW` resume guard with focused tests so the run could resume after row 141,528 without reprocessing completed rows. Successful 2025-05 raw payloads were pruned after normalization; post-prune verification found 0 prune candidates, 496,106 parsed rows with pruned payloads, and the 4 failed export rows retaining raw payloads with `payload_retained_reason = parse_error`. Failed export rows are split-line field-count errors at raw row numbers 33,760/33,761 and 35,278/35,279, with 19 or 66 fields instead of 84. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, 0 source/import-batch mismatches, and 0 pending-prune rows. Product-facing period discovery now lists `2025-05` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-05` imports in about 13.9s and exports in about 2.3s, with exact summaries complete; a broad `2025-05` through `2026-04` search returned 25 rows over 6,099,791 all-flow records in about 1.0s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete but slow at about 18.2s over 1,585,838 records. Browser checks on port 3001 confirmed default Explorer shows the loaded range `2025-05` to `2026-04`, includes `2026-04`, excludes `2099-01`, exact `2025-05` import renders 404,234 records, exact `2025-05` export renders 91,872 records, and broad `2025-05` through `2026-04` renders 4,915,002 import records. Neon dev measured about 14 GB immediately after this batch. Focused `test:trade-normalizer` passed after the resume-guard change; run the full focused ingestion/search/provenance test set and typecheck after the 2025-01 batch or earlier if more code changes are needed. Recommendation: `2025-04` is safe as the next single-month batch with the same constraints, but monitor exact import and narrowed broad search latency plus any transient Neon upsert failures.
- The next historical backfill batch ran on 2026-06-05 for `2025-04` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload showed 168 archived objects, 86 missing non-current objects, 0 remote mismatches, 0 unsafe objects, and 0 errors; after guarded uploads of the 4 official raw objects, 4 working objects, and refreshed historical source-manifest snapshot, R2 preflight showed 177 archived objects, 77 missing non-current objects, 0 mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-04 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 390,891 parsed import rows, 112,765 parsed export rows, and 8 retained export parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-04` and `TRADE_RECORD_BATCH_SIZE=100`, creating 503,656 `trade_records` exactly matching parsed raw rows, touching 17,653 anonymous participants, and touching 2,463 logistics parties. Successful 2025-04 raw payloads were pruned after normalization; post-prune verification found 0 prune candidates, 503,656 parsed rows with pruned payloads, and the 8 failed export rows retaining raw payloads with `payload_retained_reason = parse_error`. Failed export rows are split-line field-count errors at raw row numbers 19,614/19,615, 62,872/62,873, 72,550/72,551, and 97,869/97,870, with 19 or 66 fields instead of 84. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, 0 source/import-batch mismatches, and 0 pending-prune rows. Product-facing period discovery now lists `2025-04` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-04` imports in about 2.3s and exports in about 0.8s, with exact summaries complete; a broad `2025-04` through `2026-04` all-flow search returned 25 rows over 6,603,447 records in about 6.3s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 26,898 records in about 1.4s. Browser checks on port 3001 confirmed default Explorer shows the loaded range `2025-04` to `2026-04`, includes `2026-04`, excludes `2099-01`, exact `2025-04` import renders 390,891 records, exact `2025-04` export renders 112,765 records, and broad `2025-04` through `2026-04` renders 5,305,893 import records. Neon dev measured about 16 GB immediately after this batch. Recommendation: `2025-03` is safe as the next single-month batch with the same constraints, while continuing to monitor broad multi-month list latency and recurring export split-line parse errors.
- The next historical backfill batch ran on 2026-06-05 for `2025-03` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload showed 176 archived objects, 86 missing non-current objects, 0 remote mismatches, 0 unsafe objects, and 0 errors; after guarded uploads of the 4 official raw objects, 4 working objects, and refreshed historical source-manifest snapshot, R2 preflight showed 185 archived objects, 77 missing non-current objects, 0 mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-03 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 385,873 parsed import rows, 100,956 parsed export rows, and 0 parse-error rows. Normalization used `NORMALIZE_PERIOD=2025-03` and `TRADE_RECORD_BATCH_SIZE=100`, creating 486,829 `trade_records` exactly matching parsed raw rows. The first normalizer attempt completed export and import through raw row 57,744 before a transient insert/database failure; resume with `NORMALIZE_START_FLOW=import` and `NORMALIZE_START_ROW=57744` passed the failure boundary and completed, but the current resume helper also replayed export before import, so future resume ergonomics should be improved separately. The completed resume touched 15,269 anonymous participants and 3,493 logistics parties. Successful 2025-03 raw payloads were pruned after normalization; export pruning completed 100,956 rows, import pruning stopped once after 160,000 committed rows because of a transient update failure, then a scoped retry pruned the remaining 225,873 rows. Post-prune verification found 0 prune candidates, 0 pending-prune rows, and no retained raw payloads for 2025-03 because there were no parse failures. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, and 0 source/import-batch mismatches. Product-facing period discovery now lists `2025-03` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-03` imports in about 2.1s and exports in about 1.2s, with exact summaries complete; a broad `2025-03` through `2026-04` all-flow search returned 25 rows over 7,090,276 records in about 0.7s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 28,989 records in about 1.1s. Browser checks on port 3001 confirmed default Explorer shows `2026-04` and not `2099-01`, exact `2025-03` import renders 385,873 records, exact `2025-03` export renders 100,956 records, and broad `2025-03` through `2026-04` renders 5,691,766 import records. Neon dev measured about 17 GB immediately after this batch. Recommendation: `2025-02` is safe as the next single-month batch with the same constraints, while continuing to monitor transient Neon insert/prune failures and improving normalizer resume semantics later.
- The next historical backfill batch ran on 2026-06-05 for `2025-02` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload showed 184 archived objects, 86 missing non-current objects, 0 remote mismatches, 0 unsafe objects, and 0 errors; after guarded uploads of the 4 official raw objects, 4 working objects, and refreshed historical source-manifest snapshot, R2 preflight showed 193 archived objects, 77 missing non-current objects, 0 mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-02 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 354,974 parsed import rows, 98,416 parsed export rows, and 6 retained export parse-error rows. Failed export rows are recurring split-line field-count errors at raw row numbers 12,038/12,039, 51,615/51,616, and 52,205/52,206, with 19 or 66 fields instead of 84. Normalization used `NORMALIZE_PERIOD=2025-02` and `TRADE_RECORD_BATCH_SIZE=100`, creating 453,390 `trade_records` exactly matching parsed raw rows. The first normalizer attempt completed export and import through raw row 291,684 before a transient database failure in logistics-party link cleanup; resume with `NORMALIZE_START_FLOW=import` and `NORMALIZE_START_ROW=291684` skipped export and completed import, touching 2,515 anonymous participants and 739 logistics parties in the resumed segment. Successful 2025-02 raw payloads were pruned after normalization; post-prune verification found 0 prune candidates, 453,390 parsed rows with pruned payloads, and the 6 failed export rows retaining raw payloads with `payload_retained_reason = parse_error`. During this batch an initial raw-loader command used the source-file manifest env var instead of `RAW_TRADE_ROW_SOURCE_FILENAMES`, which briefly touched the first 100 March 2026 import/export raw rows and import-batch counters; this was detected before continuing, repaired from preserved working source files, and verified so the first 100 March 2026 import/export rows are back to `full_postgres` / `dev_sample` and March 2026 batch counters match their full source counts. To prevent repeat cross-period pruning risk, `scripts/ingest/prune-raw-trade-row-payloads.ts` now supports `RAW_ROW_PRUNE_PERIOD=YYYY-MM`, with focused pruner tests passing, and future backfill pruning should always set that period scope. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, 0 source/import-batch mismatches, and 0 pending-prune rows. Product-facing period discovery now lists `2025-02` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 25 rows for exact `2025-02` imports in about 2.3s and exports in about 1.1s, with exact summaries complete; a broad `2025-02` through `2026-04` all-flow search returned 25 rows over 7,543,666 records in about 5.1s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 30,711 records in about 1.2s. Browser checks on port 3001 confirmed default Explorer shows `2026-04` and not `2099-01`, exact `2025-02` import renders 354,974 records, exact `2025-02` export renders 98,416 records, and broad `2025-02` through `2026-04` renders 6,046,740 import records. Neon dev measured about 18 GB immediately after this batch. Recommendation: `2025-01` is safe as the next single-month batch with the same constraints, but always use `RAW_TRADE_ROW_SOURCE_FILENAMES` for raw loading, always set `RAW_ROW_PRUNE_PERIOD=2025-01` for pruning, and continue monitoring transient Neon failures.
- The final 2025 historical backfill batch ran on 2026-06-05 for `2025-01` only. Official datos.gob.cl import main, export main, export bultos, and export transport-document resources were acquired serially, extracted, checksum-manifested, and archived to private R2 with snapshot-mode source-manifest preservation. Snapshot R2 preflight before upload showed 192 archived objects, 86 missing non-current objects, 0 remote mismatches, 0 unsafe objects, and 0 errors; after guarded uploads of the 4 official raw objects, 4 working objects, and refreshed historical source-manifest snapshot, R2 preflight showed 201 archived objects, 77 missing non-current objects, 0 mismatches, 0 unsafe objects, and 0 errors. Neon dev seeded only the 2025-01 main import/export `dataset_resource` source files. Preflight on the two main files had 0 blockers, import sampled at 178 fields, export sampled at 84 fields, and only known coded-field/payload-retention warnings. Raw load used `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings` and loaded 382,992 parsed import rows, 127,470 parsed export rows, 20 retained import parse-error rows, and 34 retained export parse-error rows. Import failures are field-count errors with 135, 136, or 137 fields instead of 178; export failures include 63-field rows plus recurring 19/66 split-line rows instead of 84. The total failure rate stayed low and did not suggest a layout incompatibility, so parsed rows were normalized and failed payloads were retained for review. Normalization used `NORMALIZE_PERIOD=2025-01` and `TRADE_RECORD_BATCH_SIZE=100`, creating 510,462 `trade_records` exactly matching parsed raw rows, touching 17,822 anonymous participants, and touching 2,382 logistics parties. Successful 2025-01 raw payloads were pruned after normalization with `RAW_ROW_PRUNE_PERIOD=2025-01`; post-prune verification found 0 prune candidates, 510,462 parsed rows with pruned payloads, and the 54 failed rows retaining raw payloads with `payload_retained_reason = parse_error`. Provenance checks found 0 orphaned trade records, 0 duplicate raw links, 0 parsed raw rows missing trade records, 0 source/import-batch mismatches, and 0 pending-prune rows. Product-facing period discovery now lists `2025-01` through `2026-04`, while Explorer still defaults to `2026-04` and excludes the `2099-01` smoke-test period. Service smoke checks returned 50 rows for exact `2025-01` imports over 382,992 records in about 2.6s and exports over 127,470 records in about 0.9s; a broad `2025-01` through `2026-04` all-flow search returned 50 rows over 8,054,128 records in about 2.5s with bounded summary; a narrowed `hsCodePrefix=8471` plus China search stayed complete over 32,689 records in about 1.3s. Browser checks on port 3001 confirmed default Explorer shows `2026-04` and not `2099-01`, exact `2025-01` import renders 382,992 records, exact `2025-01` export renders 127,470 records, and broad `2025-01` through `2026-04` renders 6,429,732 import records. Neon dev measured about 19 GB immediately after this batch. The completed 2025-05 through 2025-01 run added 2,450,443 parsed raw rows, 2,450,443 normalized trade records, and 2,450,443 pruned successful raw payloads; 72 failed parse rows remain retained across the five-month batch. Final focused validation passed: `test:aduana-load-preflight`, `test:raw-row-loader`, `test:trade-normalizer`, `test:raw-row-pruner`, `test:source-provenance`, `test:trade-search`, `test:source-file-manifest`, and `typecheck`. Recommendation: `2024-12` is operationally safe to attempt next with the same one-month-at-a-time standards, but keep watching January-style import field-count failures, recurring export split-line failures, broad-range Postgres latency, and Neon storage growth.
- `docs/research/CHILE_ADUANA_PARSE_FAILURE_AUDIT_2025.md` records the read-only audit of the 72 retained parse failures from the 2025-05 through 2025-01 backfill. `npm run inspect:aduana-parse-failures` summarizes the current failure patterns and confirms all failed rows retain payloads. The audit found the failures are not critical for Explorer/product trust: failure rate is 0.00294%, normalized records equal parsed raw rows, and provenance/integrity checks remain clean. Export `19 + 66` adjacent split-line pairs are likely recoverable later with a conservative parser path, but January export 63-field rows and January import 135-137-field rows should stay retained pending more evidence. Recommendation remains to proceed to `2024-12` one month at a time, stopping only if failure volume/patterns materially worsen or integrity checks fail.
- Product-facing Chile Aduana coverage now targets `2021-01` through the latest available product-facing month, matching the observed DataSur Chile D-Comex/Aduanas Detalladas coverage window in local research. Do not load or expose pre-2021 Aduana files as product-facing Explorer data for now; keep older official/historical files for research, parser validation, or internal evidence unless a later decision expands the product coverage window.
- Local DataSur research materials were reviewed as competitive/product research only. `docs/research/DATASUR_PRODUCT_RESEARCH_NOTES.md` summarizes the inspected screenshots and sample export shape, and `docs/research/DATASUR_DUANERA_UX_DATA_PLAN.md` turns those observations into a Duanera-specific plan. Key conclusion: Duanera already has the core commercial search foundation, but DataSur-style logistics/payment/package fields split into three categories that must stay separate: existing normalized Aduana fields suitable for table view modes, raw-only operational context that requires controlled row reconstruction before detail-page display, and future schema/model fields that need separate approval. Do not copy DataSur UI/wording or treat any possible company fields as verified Aduana legal identity.
- A no-new-months product UX pass implemented the first high-return DataSur-inspired items without loading data, changing schema, or adding identity claims. Product/source descriptions now use a presentation-only friendly formatter for Explorer tables, detail panels, and normalized export columns while preserving exact raw source text in provenance/source views and adding exact source text to product/provenance exports. `/sources` now has a read-only coverage table from existing `trade_records` / `raw_trade_rows`, showing complete, partial, and missing months from 2021-01 through the latest loaded product-facing period; missing months are labeled as pending load, not errors. `/explorer` now has local-browser saved searches/history and a visible export preview with row cap, columns, applied filters, warnings, and download state. Saved searches are localStorage-only for now; no auth, billing, persistent schema, or new ingestion was introduced.
- Public product text cleanup is now centralized in `src/text/public-text.ts`. Raw Aduana/source fields remain unchanged, while public product descriptions, product attributes, operational source context, secondary product samples, and normalized product export columns use cleaned display text. Exact raw source text remains available only in source/provenance contexts such as “Texto fuente exacto” and exact source export columns. Product search query parsing and ingestion-side `product_search_text` generation now share the same accent-insensitive normalizer, so past and future Explorer product searches treat `tapicería` and `tapiceria` the same. Explorer product search now also splits `q` into useful words and requires all terms to match, so queries such as `confección tapicería` and `poliester tapiceria` can find records even when those words are separated in the source text.
- `npm run inspect:public-text-quality` is now available as a read-only public text audit. It scans bounded top grouped product descriptions and product attributes for a selected period/flow, reports raw text, cleaned display text, affected fields, counts, and example record links, and avoids returning huge Neon HTTP responses by capping grouped fragments per source area. Use it before adding new public cleanup rules; raw Aduana values are still not rewritten. The shared public cleaner also now preserves dotted code-like values such as `I.D.D.T.-F` and restores common accents such as `común` / `demás`.
- `npm run inspect:operational-fields-coverage` is now available as a read-only audit for DataSur-style logistics/payment/package fields. It samples records by selected period/flow, batch-reconstructs pruned raw rows from preserved local/R2 working files when needed, reuses the same operational field display catalog as the Explorer detail API, and reports coverage, examples, reconstruction status counts, and recommendations (`show_now`, `detail_only`, `needs_cleanup`, `do_not_use_yet`). Operational field display now decodes official existing code tables for sale clauses and transport-company country (`1 · Costos, seguro y flete`, `997 · Chile`) and uses explicit source-code fallback for unknown code fields (`Código Aduana 32`, `Código Aduana 74: 1`) instead of showing naked numbers. Payment form and bulto type still need an official label source before promotion beyond detail context. These are logistics/document parties only; do not treat carrier or document-emitter fields as importer/exporter legal identity.
- `/trade-records` now has URL-only table view modes through `view=commercial|logistics|product|provenance`. The default commercial view keeps the existing business scan order, while logistics, product/HS, and provenance views reorder the same normalized service fields to emphasize operational route context, product/value context, or source/batch/payload traceability. View switches preserve current filters through the shared trade-record link helper and intentionally clear stale cursor/offset pagination state. No raw reconstruction, schema fields, or DataSur-derived proprietary UI were added; raw-only carrier, manifest, package, payment, and warehouse fields remain future reconstruction/model work.
- `/explorer` now has a compact results-first table pass. The default commercial table hides the redundant operation column, uses tighter column widths with Product as the priority column, shows product descriptor/source-reference metadata in two compact lines, uses shorter row/header spacing, moves summary/saved-search/export-preview panels below the table, collapses secondary filters behind “Más filtros”, improves pagination text, and shows a clear selected-row checkmark. This pass changed only read/display UI and the read-only list projection needed for product attributes; it did not load months, rewrite data, change schema, or add identity claims.
- `/explorer` default table terminology is now more explicit and less internal. The default scan view uses `Partida arancelaria`, flow-aware `ID importador`/`ID exportador`, `País origen`/`País destino`, `Puerto desembarque`/`Puerto embarque`, and `US$ CIF`/`US$ FOB`; it no longer shows default `Fuente` or sparse `Peso bruto` columns. Source/provenance remains available through the renamed `Trazabilidad` view and detail/export contexts. Every table column now has a small help icon with hover/focus explanation, and Explorer table rows compact `kg netos` to `kg` only in the table while keeping the full unit available in detail/export/source contexts. No data load, stored-record rewrite, schema change, or company identity claim was introduced.
- `/explorer` record detail drawer now matches the cleaner table vocabulary. The drawer tab formerly labeled `Fuente` is now `Trazabilidad`; the header and summary use `Partida arancelaria`, `ID importador`/`ID exportador`, flow-aware country and port labels, and `US$ CIF`/`US$ FOB`. The summary prioritizes date, flow, arancel, anonymous Aduana ID, country, port, value, quantity, and status. Participants now state plainly that legal names/RUTs are not present and no inferred identities are shown. Source/raw/parser/payload details remain in `Trazabilidad`; operational/document fields remain logistics/source context only. This was a UI/display pass only: no data load, raw rewrite, schema change, or company identity claim.
- `/explorer` filter controls now use a more compact, results-first layout. The primary filter row shows only the participant ID field relevant to the selected flow (`ID importador` for imports, `ID exportador` for exports), so inactive IDs are not resubmitted accidentally. Advanced filters use clearer labels such as `País origen`, `País destino`, `Vía transporte`, flow-aware `CIF`/`FOB` value ranges, and `Peso bruto`; active filter chips are smaller and use the same vocabulary. This was UI/form behavior only: no data load, stored-data rewrite, schema change, or identity claim.
- `/explorer` period filtering now uses a compact month-range picker instead of free-text `Desde`/`Hasta` inputs. The picker is UI-only, uses already loaded product-facing months, and still submits the existing `periodFrom` / `periodTo` query parameters understood by the trade search layer.
- `/explorer` results toolbar now keeps export controls compact and table-first. The old full-width export preview section was replaced by an `Exportar` dropdown that shows export status, warnings, row cap, and selectable XLSX columns. Selected columns are sent through the existing export route as a `columns` query param, validated against the active table view, and applied to CSV/XLSX output in view order. This was UI/export-route behavior only: no data load, schema change, stored-data rewrite, or company identity claim.
- Export column names now use clearer trade spreadsheet vocabulary without changing table labels or raw data. `Periodo` is exported as `Año` and `Mes`, old `columns=period` links are normalized to `year,month`, and flow-aware export labels distinguish `ID importador/exportador Aduana`, `País origen/destino`, `Puerto embarque/desembarque`, and `US$ CIF/FOB`. The export still avoids `RUT probable`, `Probable importador/exportador`, and any legal identity claim.
- `/explorer` country filtering now supports multiple countries in one search/export. `originCountry` and `destinationCountry` URLs accept comma-separated values such as `336,317` and repeated params; the parser keeps backward-compatible single-code fields while adding `originCountryCodes` / `destinationCountryCodes`. Country filters use OR inside the country group and still AND with other filters such as HS code and period. Explorer advanced filters show compact checkbox dropdowns, active chips list all selected countries, and export preview/download links preserve the same combined country filter.
- `/trade-records` and `/explorer` now have controlled CSV/XLSX export options. `src/trade/trade-record-export.ts` defines the shared MVP policy: exact flow, bounded month or month range, at least one narrowing product/commercial/geography/logistics/source/range filter, known result count, and at most 500 rows. Small filtered multi-month results are allowed, while broad, missing-period, empty, over-cap, and invalid-column exports remain blocked. `/api/trade-records/export-preview` returns the selected table view, applied filters, row cap, columns, caveats, blocking warnings, and estimated rows only after the query is structurally safe enough to count. `/api/trade-records/export` downloads CSV, and `/api/trade-records/export-xlsx` plus `/api/explorer/export-xlsx` download bounded workbooks with `Registros`, `Resumen`, and `Filtros y trazabilidad` sheets. Routes strip table pagination params, use formula-safe spreadsheet text, export only normalized table-view fields, and never export raw payloads, local paths, R2 keys, bucket URLs, credentials, or legal identity claims from anonymous Aduana correlatives.
- `/participants/importer/[id]` and `/participants/exporter/[id]` now provide anonymous Aduana participant profile pages from existing product-facing `trade_records`. Profiles show records, total flow-aware CIF/FOB, active months, monthly activity, top partidas/product groups, top countries, top ports, top aduanas, and recent records while explicitly avoiding legal identity claims. Exporter profiles match both primary and secondary Aduana exporter correlatives. Explorer participant ID cells link to these pages, and row-click handling now ignores nested links/buttons so profile links do not open the detail drawer. No data load, stored-record rewrite, schema change, or identity inference was introduced.
- `/logistics-parties/[id]` now exists for Aduana transport/document parties. The schema tables are `source_logistics_parties`, `source_logistics_party_aliases`, and `trade_record_logistics_party_links`, generated in `drizzle/20260604235832_mature_jigsaw/` and applied to Neon dev. Supported roles are `issuer` (`Emisor documento transporte`: import `NOMEMISOR`, export `NOMBREEMISORDOCTRANSP`) and `carrier` (`Compañía de transporte`: import `GNOM_CIA_T`, export `NOMBRECIATRANSP`). These profiles are explicitly not importer/exporter legal identity. `db:normalize:trade-sample` writes logistics links for future normalization, and `db:backfill:logistics-parties` can backfill existing pruned rows by reconstructing from preserved source files. `/api/explorer/records/[id]` now returns `logisticsPartyLinks`, and the Explorer record drawer renders source logistics names as links to `/logistics-parties/[id]` when a record-level link exists. `/explorer` advanced filters now expose `Entidad logística` and optional `Rol logístico`, using the existing `logisticsParty` / `logisticsRole` query params and active chips. Bounded dev backfills now cover June 2025 broadly plus April 2026 samples: on 2026-06-04, `2026-04` import scanned 5,000 records and linked 9,996 appearances; `2026-04` export scanned 5,000 records and linked 9,999 appearances. Full product-facing backfill remains a follow-up; run it month/flow scoped with `DUANERA_DB_TARGET=dev` and explicit limits before broad use.
- A follow-up logistics-party implementation pass on 2026-06-04 added `npm run inspect:logistics-party-coverage`, `/api/logistics-parties/search`, and an Explorer logistics-party typeahead backed by the service layer. The typeahead keeps one `logisticsParty` query param, shows selected parties in active chips, caps default filter options, and preserves a selected party even when it is outside the default top list. `/logistics-parties/[id]` now has explicit role filter links for `Todos los roles`, `Emisor documento transporte`, and `Compañía de transporte`. Browser validation on port 3001 confirmed the advanced `Entidad logística` textbox is accessible, Maersk search/select submits the party UUID and shows `Entidad logística: Maersk`, and the profile role nav correctly routes to `?role=carrier`. The coverage report currently shows April 2026 as bounded sample coverage only: import 5,000 linked records / 9,996 links and export 5,219 linked records / 10,436 links. A full April import attempt with a 10k chunk was too slow and was stopped; the backfill script now caches local source-file line readers across chunks to avoid rereading from the start, but full logistics-party coverage remains a runtime/operations follow-up. Use month/flow scoped runs and avoid broad unbounded backfills until runtime is reviewed.
- `docs/research/DUANERA_EXPORT_REPORTING_ROADMAP.md` records the next export/reporting direction after the export MVP. Bounded CSV/XLSX is now implemented. Larger async exports, saved export history, quotas, persistent presets, and production export permissions remain auth/billing/production-policy work; raw-only logistics or identity evidence exports remain schema/model/evidence work.
- `docs/research/CHILE_ADUANA_CODE_TABLE_REMEDIATION_PROPOSAL.md` records the evidence-backed remediation proposal and dev implementation for the code-table blocker. A 2026-06-01 evidence pass found official Anexo 51-11 update evidence for all March 2026 nonzero port gaps: `225`, `817`, `818`, `819`, `820`, `821`, `822`, `823`, `824`, `826`, and `827`, covering 85 relevant-port records. `scripts/remediation/aduana-port-code-remediation.ts` inserted those 11 rows into dev `chile_aduana:puertos` with `review_status = reviewed_official_update` and `metadata.remediation_id = chile_aduana_ports_anexo_51_11_2026_06_01`. A follow-up dry-run returned 11 noops. A 2026-06-02 evidence pass found exact official evidence for the remaining March+April blockers: live Aduana Anexo 51-1 lists Aduana `56` as `ARAUCANÍA`; BCN Resolución Exenta 2222 confirms port `825` as `Aeródromo La Araucanía`; live Aduana Anexo 51-20 lists import `MONEDA` codes `141` Zloty, `145` Baht tailandés, `147` Ringgit, `149` Rupia Indonesia, and `157` Leu rumano. `scripts/remediation/aduana-evidence-code-remediation.ts` inserted exactly those 7 rows into Neon dev with `review_status = reviewed_official_update` and `metadata.remediation_id = chile_aduana_evidence_codes_anexo_51_2026_06_02`; the follow-up dry-run returned 0 inserts, 0 updates, and 7 noops. `/data-quality/code-tables` now has 0 high-priority and 0 medium-priority gaps for March, April, and March+April, leaving only low-priority source-special export values (`0` and `S`). `/data-quality/load-readiness` now returns `review-first`, not `no-go`, for those scopes.
- `db:seed:code-tables` now deletes and reinserts workbook-backed `seeded` rows only. Reviewed official-update rows in `code_values`, including the 11 remediated port rows, are preserved across workbook reseeds. A dev reseed after this change inserted the 870 baseline workbook values and `npm run db:remediate:aduana-ports -- --dry-run` still returned 11 noops.
- `/sources/[id]` now includes compact period-aware QA context for that source when the source has affected import batches. It shows issue-signal counts, parser/version, source/batch anchors, source-filtered `/trade-records` links, and technical next steps without exposing storage paths or making legal/company-identity claims.
- `docs/R2_ARCHIVE_PLAN.md` defines the Cloudflare R2 source archive policy. `npm run archive:r2:plan` generates a local dry-run upload manifest for ignored `data/` files, classifies source/research/generated/disposable files, computes SHA-256 checksums, validates official raw files against source manifests, and proposes private R2 object keys. `npm run archive:r2:verify` performs read/list-only bucket access checks. `npm run archive:r2:upload` is dry-run by default and requires both `--confirm-upload` and `R2_UPLOAD_CONFIRM=upload` before it uploads.
- The first R2 archive batches have been uploaded and verified in the private `duanera-source-archive` bucket: 45 official raw source objects under `sources/` totaling 640,587,765 bytes, 12 manifest/checksum objects under `manifests/` totaling 83,091 bytes, and 40 working-file objects under `sources/` totaling 2,444,558,267 bytes. Remote verification matched object sizes and stored SHA-256 metadata for every uploaded object. Research evidence and generated validation outputs have not been uploaded yet.
- `npm run archive:r2:preflight` is now the read-only R2 archive preflight. It verifies private bucket read/list access, rebuilds the local archive plan, compares planned objects against R2 by key, size, and stored SHA-256 metadata, groups results by archive class, highlights April 2026 objects, and prints guarded upload commands for safe missing classes. It does not upload, delete, overwrite, mutate metadata, write local files, or touch the database. The current preflight found 91 archived objects, 85 missing objects, 6 remote manifest mismatches, 0 unsafe objects, and 6 errors. April 2026 is visible: the two `aduana.cl` operational April raw/working files are already archived, while 8 `datos.gob.cl` April raw/working objects are missing from R2. The 6 mismatches are source manifest/checksum files whose local contents changed after the earlier R2 upload; they require a separate reviewed manifest-refresh/immutability decision before any manifest upload attempt.
- Source manifest refresh policy is now content-addressed and non-overwriting. The legacy `manifests/cl/aduana/{source-domain}/{filename}` keys are treated as historical first-upload objects. Refreshed manifests/checksums should use snapshot keys shaped like `manifests/cl/aduana/{source-domain}/snapshots/{filename}/{sha256}/{filename}` via `npm run archive:r2:preflight -- --manifest-key-mode snapshot --pretty` and `npm --silent run archive:r2:plan -- --manifest-key-mode snapshot --pretty > /tmp/duanera-r2-upload-plan.json`. A read-only comparison of the six datos.gob.cl mismatches confirmed the local 2026 source manifest grew from 17 to 21 rows with April entries, `sha256sums.txt` grew from 34 to 42 rows with April checksums, export package evidence added three April resources, and import package evidence added one April resource. Do not overwrite the legacy manifest keys and do not add a mutable `latest` pointer without a separate reviewed policy.
- The refreshed source-manifest snapshots have been uploaded to R2 using the non-overwriting snapshot strategy. The guarded upload selected only `source_manifest` objects and uploaded 12 snapshot objects totaling 92,400 bytes. Post-upload snapshot preflight found 97 archived objects, 85 missing objects, 0 remote mismatches, 0 unsafe objects, and 0 errors; all 12 source-manifest snapshot objects verified by remote size and stored SHA-256 metadata. The remaining April R2 gaps are the 4 datos.gob.cl April official raw objects and 4 corresponding working files; research evidence and generated validation outputs remain unuploaded.

## Chile Aduana source archive context

Current local research layout:

```txt
data/sources/chile-aduana/
  datos-gob-cl/
    imports/raw/
    imports/working/
    exports/raw/
    exports/working/
    references/raw/
    references/working/
    manifests/
  aduana-cl/
    imports/raw/
    imports/working/
    exports/raw/
    exports/working/
    code-tables/raw/
    code-tables/working/
    references/raw/
    references/working/
    manifests/
```

Cloudflare R2 archive layout is planned around these prefixes:

```txt
sources/cl/aduana/{source-domain}/{group}/{yyyy/mm|historical}/{raw|working}/...
manifests/cl/aduana/{source-domain}/...
research/cl/aduana/...
```

Local source filenames are normalized to lowercase ASCII snake_case. Manifests preserve official filenames in `original_filename`, local raw names in `normalized_raw_filename`, and usable files in `normalized_working_filenames`. The old `raw-data/` staging tree was removed after all non-temporary files were confirmed by checksum to exist in `data/sources/chile-aduana/`.

For `datos.gob.cl`, `imports/` and `exports/` should contain only actual monthly or yearly trade data resources. Shared dictionaries, metadata workbooks, schemas, methodology files, and similar non-trade-data references belong under `datos-gob-cl/references/`. Use `code-tables/` only for lookup tables that decode coded values such as countries, customs offices, operation codes, currencies, units, transport modes, or ports.

Manifest raw file roles:

- `compressed_source_file`: official compressed raw package, preserved in `raw/`.
- `direct_source_file`: directly usable official raw file, preserved in `raw/` and copied into `working/`.
- `reference_file`: dictionary, metadata, schema, or methodology file preserved under `references/raw/` and copied into `references/working/`.

`working/` is now the default location for Codex, scripts, and future parsers to inspect files. `extracted/` is no longer used.

## Next recommended steps

1. Finish manifest provenance for legacy `aduana.cl` CSV files whose original package URLs are still marked `unknown`.
2. Inspect the normalized `working/` datos.gob.cl 2026 import and export files manually.
3. Create a field inventory from official metadata and sampled rows.
4. If historical identity remains critical, expand coverage around specific policy/publication-change dates. January 2003, 2010, and 2015 import/export samples do not expose legal importer/exporter names or RUTs.
5. Treat public official identity lookup as unlikely unless a specific lawful/licensed source is identified. If such a source is found, keep it as a separate evidence layer and rerun `python3 scripts/research/chile_aduana_identity_validation.py --include-ckan` without a row cap.
6. If company intelligence remains in scope, use `/research/identity-inference` and `npm run research:identity-evidence -- --export --limit=5` to build a manually reviewed validation set before making any user-facing precision claim. Strong March 2026 evidence exists in product attributes and some export bulto companion marks; importer evidence remains weaker and transport/document parties must stay non-identity context.
7. Review and commit the current internal data-quality dashboard and QA drilldown workflow after checks and browser smoke remain clean.
8. Before loading additional months, run `npm run preflight:aduana-load` on the candidate source rows/files first. If it has 0 blockers, run a small real-file dev validation with `RAW_ROW_PAYLOAD_RETENTION=errors_and_warnings`, then normalize and prune with tight limits. The synthetic smoke path passed, but the next check should use actual Aduana parsed rows without touching existing March `full_postgres` payloads.
9. Keep company identity tables deferred until a separate evidence-backed identity pass is requested.
10. Run `npm run archive:r2:plan -- --pretty > /tmp/duanera-r2-upload-plan.json` before any first R2 upload, review warnings, and keep the bucket private with no public `r2.dev` or custom-domain access.
11. Do not promote migrations or sample data to production until the dev sample is reviewed and accepted.
